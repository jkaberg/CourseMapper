package com.coursemapper.ui.designsystem

import androidx.compose.material3.Typography

/** Material 3 defaults for now, kept here so the theme owns its typography. */
val CourseMapperTypography = Typography()
